# Experiment Reports

Markdown reports summarizing each round's findings. These are review artifacts
for human feedback — separate from `experiments/` which contains scripts and
raw JSON results.

Reports are also copied to the Obsidian vault for cross-project reference.
